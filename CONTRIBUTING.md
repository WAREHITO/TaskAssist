# 開発時のルール

本プロジェクトの独自コードへのライセンスは未指定です。利用・改変・再配布の許諾条件や外部からの変更提案の受入条件は、管理者と確認してください。

変更を行う場合は `AGENTS.md`、`SPEC.md`、`docs/QUALITY_GATES.md` を読み、架空データだけで検証してください。変更範囲を絞り、試験後に別コンテキストでレビューし、指摘を修正して再試験します。

`tools/build.ps1` が自動試験の入口です。`dotnet test` だけを実行して合格と報告しないでください。画面や実Outlookなど、未実行の確認は未実行と明記します。

公開するファイルを追加・削除したら `source-files.txt` も更新します。実際の業務データ、生成物、ローカルログは追加しません。依存関係を更新する場合はロックファイルの差分と第三者ライブラリの表示を確認します。
