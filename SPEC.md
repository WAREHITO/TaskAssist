# 現在の仕様 — 1.0.0-rc.2 接続検証版

この文書は公開用ソースに実装されている範囲を説明します。将来構想や個人向けの元の仕様書全体について、実装済みとは主張しません。

## タスクと期限

用件名のみで登録できる。期限は「不明」「なし」「日付」「日時」を区別する。期限を過ぎても自動完了せず、本人による訂正を履歴に残す。日付の判定は日本時間を使う。

現在の作業、候補、保留、返答待ち、判断待ち、完了・取りやめの記録を扱う。作業は勝手に切り替えない。期限とは別に、次に確認する日と作業候補の日を持つ。履歴からの取消しは、より新しい変更を上書きする場合に拒否する。

## カレンダーと回答集め

案件の受付日から回答期限までを色で示す。例えば架空の「9月21日受付・10月31日期限」は両月に期間を表示する。複数案件、月をまたぐ期間、期限不明・期限なしを区別する。

案件ごとに複数の担当を追加でき、担当別の依頼日、回答日、確認日、回答の種類・本文・補足を保持する。状態は未依頼・依頼済み・回答あり・確認済み。定型の回答や状態更新はクリックで選択する。Outlookからの受付は別に保管し、メール内容から担当別の回答や完了を勝手に確定しない。

## 所属と担当

初回は所属の表示名を設定し、空の記録で開始する。所属名の訂正と、人事異動による新しい所属の開始は別の操作とする。担当一覧の変更で過去の回答記録を書き換えない。

異動時には未完了案件ごとに「継続対応（別途登録）」「引継ぎ」「取りやめ」と補足を記録する。旧所属を退避してから新しい所属を作る。新しい所属へ案件や担当一覧を自動でコピーしない。旧所属は閲覧専用となり、検索、回答、履歴、異動時の判断を確認できる。旧所属の作業を再開しない。

文字サイズなど本人向け設定は所属別の記録から分離する。別PC・別Windowsアカウントへの移行は対象外。

## 保存と障害

SQLiteに保存し、同時更新や古い版の上書きを検出する。DPAPI CurrentUserによる保護を使い、別利用者への暗号化キーの移転を提供しない。データベース全体の暗号化ではなく、一部の識別子や状態などの管理情報は暗号化されない。

バックアップ復元は新しいファイルへ行い、検証に失敗した記録で現在の記録を上書きしない。所属の異なる記録の復元は拒否する。複数起動を制限する。

## 開発・検証の境界

開発試験は架空データのみを使う。製品の保存先と、試験用の保存先を分離する。製品のOutlook読取りと送信は、それぞれ利用者が対象・作用を確認して有効化する。初期状態で実メールを読まない。

## Outlook・定型業務

クラシックOutlookの起動済みCOMオブジェクトだけに別プロセスから接続する。配信ストアおよび接続済みの追加・共有領域のフォルダーを個別選択し、対象・開始日時・同意時刻・設定の世代を保存する。追加・共有領域と特殊フォルダーには追加確認を求める。対象外や未同期領域を監視済みと表示しない。5分を基本とした全範囲の再照合をページ単位で進め、1回50項目以内、通信8MiB以内。保存済み位置・失敗数・残件を保持し、保存に失敗した位置は進めない。本文64,000文字超などは読取不能として本人確認へ回す。

物理所在と原文の補助識別子を分ける。件名だけで統合しない。本人の期限訂正・完了・対応不要判断を再取得で変えない。本文取得状態を分類状態から独立して保持する。取得処理は原本の既読・分類・場所を変更しない。本人が原本をOutlookで開く操作は別で、既読に変わる可能性を説明する。

明日などは受信日時の日本時間から未確定候補を作る。年なし・期間・緊急語句は具体的な締切へ自動確定しない。原文抜粋・方法・対象と本人確認を記録する。定型規則は差出人、件名先頭、フォルダー、有効日を限定し、本文全体一致で1箇所のISO日付だけ自動確定できる。採用後の新規受付だけに作用する。複数規則一致は保留する。

毎日・毎週・毎月の発生日台帳を保存する。月末短縮、本人設定の業務日・休暇を使い、停止分は各回/最新のみ/確認待ちを選ぶ。同期限・緊急案件・利用可能時間超過を理由付きで表示し、現在作業を切り替えない。

添付は本人選択のローカルフォルダーへ保存だけ行い、元の番号・名前・サイズ・更新日時を再確認する。自分の処理ID付き名前で上書きしない。本人通知は確認した送信アカウントと同じ1アドレスへ、変化時に1日1回まで。原文・引用・添付を通知に入れない。下書きは個別承認し宛先空欄で保存する。送信の戻り値はOutlookへの処理受付であり配送確認ではない。

実行前に外部処理台帳を永続化する。結果不明は自動再実行しない。再起動・復元・異動で古い待機ジョブを再実行しない。規則・許可・設定の前後を保護した履歴に残す。

## 保持と復旧

日次退避を個別に有効化でき、既定の保持は7世代。この機能の管理台帳にある退避だけを整理する。手動・更新前退避は別に保持する。復元は別ファイルへ検証後に切り替え、外部処理を停止する。原文削除は期間と件数を確認し、原文由来の期限根拠・履歴も除去する。確定した期限、案件、本人メモ、担当回答は保持する。旧バックアップにある原文はこの操作では消えない。

## 実装・検証の境界

実機メール操作・利用者受入に未検証項目がある。[対応表](docs/IMPLEMENTATION_STATUS.md)に、実装済み・部分実装・未検証を分けて記載する。追加・共有領域はOutlookに既に接続されているものを個別選択する。旧版0.4へ戻す場合は更新前の形式3の退避を画面から検証復元して終了し、保持した旧版を本人が起動する。接続検証版の公開を当初全要件の合格とは扱わない。

実装変更は試験後に別コンテキストでレビューし、指摘を修正して再試験する。同じAIの別コンテキストでのレビューは人間による独立監査とは区別する。実行していない画面・外部接続・利用者受入試験を合格扱いしない。
